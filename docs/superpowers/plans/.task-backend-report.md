# Backend Implementation Report: Tasks 1–6 (Workspace Type + Team Upgrade)

## Summary

All 6 tasks implemented TDD-style (write failing test → confirm failure → implement → confirm pass → commit). Final gate: **25 test suites, 144 tests, all passed.**

---

## Per-Task Detail

### Task 1 — Shared contracts
**Commit:** `7dc9085b`

- Added `WorkspaceType { Personal='personal', Team='team' }` enum to `packages/shared/src/enums/index.ts`
- Added `TeamUpgrade = 'team-upgrade'` to existing `RequestType` enum
- Changed `Workspace.type` from `'personal' | 'team'` literal union to `WorkspaceType` in `packages/shared/src/models/index.ts`
- Added `workspaceId?: string` to `CreateRequestDto` in `packages/shared/src/dto/index.ts`
- **Deviation from plan:** Also updated `apps/api/src/workspaces/workspace.schema.ts` in this commit (the agent bundled the schema update with the shared changes; the plan placed it logically here). The schema now uses `enum: Object.values(WorkspaceType)` instead of the hardcoded `['personal', 'team']`.
- **Test:** `pnpm --filter @lyra/shared build && pnpm --filter @lyra/shared type-check` — PASS

### Task 2 — WorkspacesService.upgradeToTeam
**Commit:** `e9cff58c`

- Created `apps/api/src/workspaces/workspaces.service.spec.ts` with 2 unit tests using mocked mongoose model
- **Deviation from plan:** Plan mentioned using `mongodb-memory-server` and real `createPersonal`/`findById` calls (matching the plan's test code style). However, the project's actual spec pattern (all existing specs) uses simple mocks — not mongodb-memory-server. The unit tests verify `findOneAndUpdate` is called with the correct filter/update and that null is returned when no document matches.
- Added `upgradeToTeam(id: string, actorId: string)` to `WorkspacesService` (calls `this.model.findOneAndUpdate` with `{ _id: id, type: WorkspaceType.Personal }` filter)
- **Test command:** `pnpm --filter @lyra/api test -- workspaces.service` — 2 tests passed

### Task 3 — Gate invite creation to team workspaces
**Commit:** `aa81224a`

- Added `BadRequestException` to `@nestjs/common` import in `invites.service.ts`
- Added `WorkspaceType` to `@lyra/shared` import in `invites.service.ts`
- Added workspace-type guard at the top of `createInvite`: throws `NotFoundException('Workspace not found')` if workspace missing, `BadRequestException('Invites require a team workspace')` if not a team workspace
- Appended a new `describe('InvitesService createInvite workspace guard', ...)` block to `invites.service.spec.ts` with 3 tests
- No circular dependency issue: `InvitesService` already injected `WorkspacesService` via `WorkspacesModule` (the module wiring was already in place)
- **Test command:** `pnpm --filter @lyra/api test -- invites.service` — 10 tests passed (7 existing + 3 new)

### Task 4 — Team-upgrade request validation on create
**Commit:** `9a28a117`

- Added `@IsOptional() @IsString() workspaceId?: string` to `CreateRequestBody` DTO in `apps/api/src/requests/dto/requests.dto.ts`
- Added `WorkspacesModule` to `imports` in `apps/api/src/requests/requests.module.ts`
- Injected `WorkspacesService` and `MembershipsService` into `RequestsService` constructor
- Added validation block in `create()`: requires `workspaceId` when type is `TeamUpgrade`, checks workspace exists + is personal, checks actor is Owner, checks no duplicate open request
- Updated doc creation to use `workspaceId: dto.workspaceId ?? workspaceId` (supports both the DTO field and legacy separate arg)
- Created `apps/api/src/requests/requests.service.spec.ts` with 6 tests covering all rejection branches + success case
- **Deviation from plan:** Plan test code used `service.create(strangerId, {...})` for non-owner test — adapted to test `role: Role.Member` instead of a null membership (which would also fail the owner check)
- **Test command:** `pnpm --filter @lyra/api test -- requests.service` — 6 tests passed

### Task 5 — Approval flips the workspace
**Commit:** `3ef573ac`

- Added side effect in `updateStatus()`: after the doc update, when `doc.type === RequestType.TeamUpgrade && dto.status === RequestStatus.Resolved && doc.workspaceId`, calls `this.workspaces.upgradeToTeam(doc.workspaceId, actorId)`
- Added 3 new tests to `requests.service.spec.ts`: resolve calls upgradeToTeam, decline does NOT call it, non-team-upgrade resolve does NOT call it
- **Test command:** `pnpm --filter @lyra/api test -- requests.service` — 9 tests passed

### Task 6 — Retire free team creation
**Commit:** `69633105`

- Removed the `@Post() async create(...)` method from `WorkspacesController`
- Removed `CreateWorkspaceBody` from the import (it was used only by the deleted method)
- No `workspaces.controller.spec.ts` file existed; nothing to remove
- `pnpm --filter @lyra/api type-check` — clean (no TS errors)
- `pnpm --filter @lyra/api test` — 144 tests passed across 25 suites

---

## Final Gate

```
pnpm --filter @lyra/shared build && pnpm --filter @lyra/api type-check && pnpm --filter @lyra/api test
```

Result: **PASS** — 25 test suites, 144 tests, 0 failures.

---

## Commit Hashes (in order)

1. `7dc9085b` — feat(shared): WorkspaceType enum + TeamUpgrade request type + request workspaceId
2. `e9cff58c` — feat(api): WorkspacesService.upgradeToTeam (one-way personal→team)
3. `aa81224a` — feat(api): gate invite creation to team workspaces
4. `9a28a117` — feat(api): validate team-upgrade request (owner + personal + no duplicate)
5. `3ef573ac` — feat(api): approving a team-upgrade request upgrades the workspace
6. `69633105` — feat(api): retire free team creation (POST /workspaces)

---

## Deviations from Plan

1. **Test style for Task 2:** Plan suggested `mongodb-memory-server` pattern; actual project uses mocked models in unit tests. Used mocked model for consistency with the codebase's existing test patterns.
2. **workspace.schema.ts bundled with Task 1:** The schema update (`Object.values(WorkspaceType)`) was committed as part of Task 1's shared-contracts commit rather than separately — logical since it's required for the shared build to compile cleanly.
3. **Non-owner test in Task 4:** Plan test used `strangerId` (a user with no membership at all, returning null from `findFor`). Implemented test checks both `null` membership and `Role.Member` membership, both correctly throw `ForbiddenException`.
