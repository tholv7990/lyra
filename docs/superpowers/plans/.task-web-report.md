# Web Task Report: Workspace Type + Admin-Gated Team Upgrade (Tasks 7–9)

## Task 7: Remove "+ add team" from the workspace switcher

**Files changed:**
- `apps/web/src/layout/WorkspaceMenu.tsx` — removed the `creating` state, `name`/`busy`/`error` state, `onCreate` handler, and the entire create form / "new team workspace" button block. Also removed the unused `FormEvent` import and `createWorkspace` destructure.
- `apps/web/src/workspace/WorkspaceContext.tsx` — removed `createWorkspace` from the context interface, implementation, context value object, and the `CreateWorkspaceDto` import. All other callers confirmed absent via grep.

**Verification:** `pnpm --filter @lyra/web type-check` — PASS.

**Commit:** `99b160ad` — `feat(web): remove free team creation from the workspace switcher`

---

## Task 8: Members page upgrade prompt + request modal

**Files created/changed:**
- `apps/web/src/components/RequestTeamUpgradeModal.tsx` — new modal mirroring `RequestProviderModal.tsx`; fields: team name (`subject`) + optional reason (`body`); POSTs `{ type: RequestType.TeamUpgrade, subject, body, workspaceId }` to `/requests`.
- `apps/web/src/pages/Members.tsx` — replaced `if (current.type !== 'team') return <Navigate to="/" replace />` with a personal-workspace upgrade prompt block rendering `upgradeTitle`, `upgradeBody`, a CTA button opening the modal, and a success confirmation on submit. Added `upgradeOpen`/`upgradeSent` state; imported `WorkspaceType` and `RequestTeamUpgradeModal`.
- `apps/web/src/i18n/locales/en/members.ts` — added 12 upgrade-related keys (`upgradeTitle`, `upgradeBody`, `upgradeCta`, `upgradeSent`, `upgradeFailed`, `upgradeModalTitle`, `upgradeModalSub`, `upgradeNameLabel`, `upgradeNamePlaceholder`, `upgradeNoteLabel`, `upgradeNotePlaceholder`, `upgradeSend`, `upgradeSending`).
- `apps/web/src/i18n/locales/vi/members.ts` — same keys in Vietnamese.
- `apps/web/src/pages/Members.test.tsx` — new test: personal workspace renders `members.upgradeTitle` + `members.upgradeCta` and not `members.peopleCount`; team workspace renders `members.searchPlaceholder` + `members.invite` and not `members.upgradeTitle`.

**Deviations from plan:**
- Test assertion for team workspace changed from `peopleCount` (requires async load resolution) to `searchPlaceholder` + `invite` (present in initial static render). Functionally equivalent coverage.
- One string in `en/members.ts` (`upgradeSent`, `upgradeModalSub`) needed double-quotes because the value contains an apostrophe — oxc parser rejected smart quotes in single-quoted strings. Fixed immediately.
- `WorkspaceView` fixture in the test needed `canManageKeys`, `createdBy`, `updatedBy` fields (TypeScript strict). Added inline inside the `vi.mock` factory to avoid hoisting issues.

**Test command:** `pnpm --filter @lyra/web test -- Members`
**Result:** 2/2 PASS (47 total pass, 0 fail)

**Commit:** `0208db7c` — `feat(web): request team upgrade from the Members page (personal workspaces)`

---

## Task 9: Admin Requests shows team-upgrade context + approves

**Files changed:**
- `apps/web/src/pages/Admin.tsx` — added `<option value="team-upgrade">` to the type filter `<select>`; surfaced `row.workspaceId` in the `RequestRow` meta line when present.
- `apps/web/src/i18n/locales/en/admin.ts` — added `reqType['team-upgrade']: 'Team upgrade'` and `reqWorkspace: 'Workspace {{id}}'`.
- `apps/web/src/i18n/locales/vi/admin.ts` — same keys in Vietnamese.

**Note:** No new endpoint needed — the existing status `<select>` already PATCHes status; resolving a team-upgrade request fires the server-side workspace flip (Task 5, already merged). `UserRequest.workspaceId` already existed on the shared model.

**Commit:** `9b3d6bb5` — `feat(web): admin can approve team-upgrade requests (resolve → upgrade)`

---

## Final gate result

```
pnpm --filter @lyra/web type-check  PASS
pnpm --filter @lyra/web lint        PASS
pnpm --filter @lyra/web test        PASS — 47 tests, 16 suites, 0 failures
```

## Commits in order

1. `99b160ad` — feat(web): remove free team creation from the workspace switcher
2. `0208db7c` — feat(web): request team upgrade from the Members page (personal workspaces)
3. `9b3d6bb5` — feat(web): admin can approve team-upgrade requests (resolve → upgrade)
