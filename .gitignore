# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
*.tsbuildinfo

# turbo
.turbo/

# env (keep examples)
.env
.env.*
!.env.example

# test / coverage
coverage/

# local dev process logs
*.out.log
*.err.log

# local dev scratch (mongod data, etc.)
.devdata/

# Claude Code settings + MCP config (may hold connection strings / local prefs).
# Shared, non-secret config in .claude/ (agents, per-package CLAUDE.md) IS committed.
.claude/settings.json
.claude/settings.local.json
.claude/worktrees/
.mcp.json

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
*.local

# Temp dump from Figma MCP get_design_context (not source)
.figma-assets/

# brainstorming visual companion
.superpowers/

# Impeccable design-hook cache (per-tree, not source)
.impeccable/
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/node_modules/

# design-sync (claude.ai/design) build scratch — never commit
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/node_modules/
scripts/.auth-state.json
docs/redesign-screens/
